apiVersion: dapr.io/v1alpha1
kind: Component
metadata:
  name: order-statestore
spec:
  type: state.mysql
  version: v1
  metadata:
  - name: connectionString
    value: "root:rootpassword@tcp(mysql:3306)/"
  - name: tableName
    value: "dapr_state"
